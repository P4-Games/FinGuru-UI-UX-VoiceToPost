//import { AlgoClientConfig } from '@algorandfoundation/algokit-utils/types/network-client'
//import type { TokenHeader } from 'algosdk/dist/types/client/urlTokenBaseHTTPClient'

export const x = ""; 

/*
export interface AlgoClientConfig extends AlgoClientConfig {
  server: string
  port: string | number
  token: string | TokenHeader
  network: string
}

export interface AlgoKMDConfig extends AlgoClientConfig {
  server: string
  port: string | number
  token: string | TokenHeader
  wallet: string
  password: string
}
*/
